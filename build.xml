<?xml version="1.0" ?> 
<project name="Equivalent Exchange 3">
	
	<property file="build.properties" />
	
	<!--Pahimar, in order to continue, I need to know whether you want me to put all the properties inside build.properties or not-->
	<!--<property name="download.dir" 	value="download"/>
	<property name="mcp.version"     	value="723"/>
	<property name="forge.version"       	value="6.4.0.395"/>
	<property name="mcp.name" 	     	value="mcp${mcp.version}.zip"/>
	<property name="forge.name" 	     	value="minecraftforge-src-${release.mod.version}-${forge.version}.zip"/>-->

	<available property="forge-exists" file="${download.dir}/${forge.name}"/>
	<available property="mcp-exists" file="${download.dir}/${mcp.name}"/>

	<condition property="out-of-date">
		<not>
			<and>
				<available file="${download.dir}/${mcp.name}"/>
				<available file="${download.dir}/${forge.name}"/>
				<available file="${mcpsrc.dir}"/>
			</and>
		</not>
	</condition>

<mkdir dir="${download.dir}"/>
	<target name="clean">
		<delete file="${dir.development}\mcp\src\common\mcmod.info" />
		<delete dir="${dir.development}\mcp\src\common\ee3" />
		<delete dir="${dir.development}\mcp\reobf\minecraft" />
	</target>
	
	<target name="build">
		<copy todir="${dir.development}\mcp\src\common">
			<fileset dir="${dir.development}\source\Equivalent-Exchange-3\ee3_client\" />
			<fileset dir="${dir.development}\source\Equivalent-Exchange-3\ee3_common\" />
		</copy>
		<replace dir="${dir.development}\mcp\src\common" token="@VERSION@" value="${ee3.version}" />
	</target>
	
	<target name="recompile">
		<exec dir="${dir.development}\mcp" executable="cmd" os="Windows 7">
			<arg line="/c recompile.bat" />
		</exec>
		<exec dir="${dir.development}\mcp" executable="bash" os="Linux">
			<arg line="recompile.sh" />
		</exec>
	</target>
	
	<target name="reobfuscate">
		<exec dir="${dir.development}\mcp" executable="cmd" os="Windows 7">
			<arg line="/c reobfuscate.bat" />
		</exec>
		<exec dir="${dir.development}\mcp" executable="bash" os="Linux">
			<arg line="reobfuscate.sh" />
		</exec>
	</target>
	
	<target name="release-shared">
		<!-- Prep for the release -->
		<antcall target="clean" />
		<antcall target="build" />
		<antcall target="recompile" />		
		<antcall target="reobfuscate" />
		
		<!-- Build the jar -->
		<mkdir dir="${dir.share}\${mc.version}\${ee3.version}" />
		<jar destfile="${dir.share}\${mc.version}\${ee3.version}\ee3-universal-${ee3.version}.jar">
			<fileset dir="${dir.development}\mcp\src\common\"	includes="mcmod.info" />
			<fileset dir="${dir.development}\mcp\reobf\minecraft" />
			<fileset dir="${dir.development}\source\Equivalent-Exchange-3\resources" />
		</jar>
		
		<!-- Clean up the MCP source now that we are done -->
		<antcall target="clean" />
	</target>
	
	<target name="release">
		<!-- Prep for the release -->
		<antcall target="clean" />
		<antcall target="build" />
		<antcall target="recompile" />		
		<antcall target="reobfuscate" />
		
		<!-- Build the jar -->
		<mkdir dir="${dir.release}\${mc.version}\${ee3.version}" />
		<jar destfile="${dir.release}\${mc.version}\${ee3.version}\ee3-universal-${ee3.version}.jar">
			<fileset dir="${dir.development}\mcp\src\common\"	includes="mcmod.info" />
			<fileset dir="${dir.development}\mcp\reobf\minecraft" />
			<fileset dir="${dir.development}\source\Equivalent-Exchange-3\resources" />
		</jar>
		
		<!-- Clean up the MCP source now that we are done -->
		<antcall target="clean" />
	</target>
	
</project>